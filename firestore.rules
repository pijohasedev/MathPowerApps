rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write access to all users to restore functionality
      allow read, write: if true;
    }
  }
}
